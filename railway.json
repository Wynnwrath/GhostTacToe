{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r server/requirements.txt && cd client && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && python -m uvicorn main:socket_app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
